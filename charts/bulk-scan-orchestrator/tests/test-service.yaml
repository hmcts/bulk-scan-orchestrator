# ---
# apiVersion: v1
# kind: Pod
# metadata:
#   name: "{{ .Release.Name }}-test-service"
#   annotations:
#     "helm.sh/hook": test-success
# spec:
#   containers:
#   - name: {{ .Release.Name }}-test-service
#     image: hmcts.azurecr.io/hmcts/cnp-aks-client:az-2.0.50-kubectl-1.12.2-helm-2.11
#     env:
#       - name: SERVICE_NAME
#         value: {{ .Release.Name }}
#       - name: CONNECTION_STRING
#         valueFrom:
#           secretKeyRef: servicebus-{{ .Release.Name }}-secret
#           key: connectionString
#     command: ["sh", "-c", "[ -z $CONNECTION_STRING ] && exit 1" ]
#   restartPolicy: OnFailure