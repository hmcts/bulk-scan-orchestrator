#!groovy

@Library("Infrastructure")

def type = "java"
def product = "rpe"
def component = "bulk-scan-orchestrator"

def channel = '#rpe-build-notices'


withPipeline(type, product, component) {
  after('test') {
    sh './gradlew integration'
  }

  enableSlackNotifications(channel)
}
